# Vesper Eye для DankMaterialShell

Живой готический глаз для рабочего стола **Hyprland + DMS**. Оболочка обновляется отдельно — это плагин, не форк DMS и не набор личных dotfiles.

**Статус: порт 0.1.1 (preview), исправлена плавность взгляда.** Проверен внутри DMS **v1.6.0** в отдельной вложенной Wayland-сессии. Не объявляется проверенным на всех будущих версиях DMS. Версия 0.1.1 загружена в основной сессии Hyprland; размер виджета увеличен настройкой экземпляра. Реальный сон ноутбука и аудиореакция ещё требуют проверки.

## Что работает

- Радужка и зрачок следуют за указателем с разной инерцией; координаты учитывают экран и положение desktop-виджета.
- **Моргают отдельные веки. Глаз не сжимается:** контур, размер склеры и пропорции радужки не анимируются по высоте.
- Удержание ЛКМ закрывает веки, отпускание открывает; двойной клик вызывает краткое удивление.
- Через 10 секунд без движения — дремота верхнего века. Движение будит глаз с двойным морганием.
- Уведомления вызывают короткий взгляд в сторону всплывающего сообщения, с cooldown и уважением DND.
- Размер/положение — через desktop editor DMS; фиксированная палитра Sanguine Ivory либо цвета текущей темы.
- По умолчанию **Eco**: слежение и ручные реакции сохраняются, случайные микродвижения и музыка отключены. Для более живого поведения можно отключить Eco; на батарее он включается автоматически.

Музыкальный адаптер использует общий `CavaService` DMS только при явном включении и воспроизведении. Он выключен по умолчанию; реальный аудиосценарий пока не проверен.

## Установка

Требуются DMS 1.6.0, Quickshell с Qt 6 ShaderEffect, Hyprland и Python 3. Сетевая активность плагину не нужна.

1. Скопировать содержимое репозитория в **новый** каталог `~/.config/DankMaterialShell/plugins/vesperEye` (или соответствующий `$XDG_CONFIG_HOME`). Существующий каталог сначала сохранить; не клонировать поверх чужих файлов.
2. В DMS включить **Vesper Eye** в Plugins и добавить экземпляр через редактор desktop-виджетов. Настройки экземпляра доступны в редакторе DMS.
3. После обновления внутренних QML-компонентов может потребоваться перезапуск именно DMS в удобное время: hot reload не всегда сбрасывает кэш зависимых типов.

Готовый `shaders/eye.frag.qsb` включён. Для пересборки: `bash scripts/build-shader.sh`. Нужен Qt 6 `qsb`; пакет содержит варианты GLSL/ES/HLSL/MSL, не только SPIR-V.

## Проверки и ограничения

- `python3 -m unittest discover -s tests -p 'test_*.py' -v` — read-only IPC, явная compositor instance, DPMS, выключение cursor tracking, остановка helper.
- Qt 6 `qmltestrunner -input tests` в отдельной графической сессии — геометрия, приоритет ручного ввода, веки, дремота, реакции и завершение интерполяции.
- [Результаты runtime-проверок](docs/VALIDATION.md), [нагрузка и режимы](docs/PERFORMANCE.md).
- Нет гарантии определения полного перекрытия окнами. Не путать `visible` с occlusion.
- Helper работает только с явно указанной текущей Hyprland instance. На отключённом экране остаётся проверка DPMS примерно раз в секунду, но нет опроса курсора. При disable/lock/невидимой поверхности helper завершается.
- Нет камеры, перехвата клавиатуры, чтения содержимого уведомлений, интернет-запросов или ИИ-модели для поведения.

## Исходники и дизайн

[Поведение](docs/BEHAVIOR.md) · [Архитектура](docs/PORTING.md) · [Дальнейший план](docs/ROADMAP.md) · [Палитра](design/palette.json) · [HTML-концепт](design/style-board.html)

`legacy/noctalia/` — исходная версия четырёх файлов, сохранённая без изменений. Это архив, не зависимость плагина. HTML-концепт не заменяет нативную реализацию.

MIT; исходные notices сохранены в `LICENSE` и `legacy/noctalia/LICENSE`. Репозиторий: [Mister-EL/dms-vesper-eye](https://github.com/Mister-EL/dms-vesper-eye).
